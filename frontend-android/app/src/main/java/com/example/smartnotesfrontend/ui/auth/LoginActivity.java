package com.example.smartnotesfrontend.ui.auth;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.example.smartnotesfrontend.R;

public class LoginActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Nạp file giao diện XML của bạn vào Activity
        setContentView(R.layout.activity_login);

        // Code xử lý sự kiện nút bấm, lấy dữ liệu Email/Password sẽ viết tiếp ở đây
    }
}