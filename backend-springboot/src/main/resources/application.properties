# ===================================================================
# 1. CẤU HÌNH KẾT NỐI CƠ SỞ DỮ LIỆU POSTGRESQL
# ===================================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/db_smart_notes
spring.datasource.username=postgres
spring.datasource.password=nghiavan2512
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================================================================
# 2. CẤU HÌNH HIBERNATE / JPA
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===================================================================
# 3. CẤU HÌNH ĐẦU CỔNG CHẠY SERVER MẠNG
# ===================================================================
server.port=8080

# ===================================================================
# 4. BỔ SUNG: CẤU HÌNH TRẠM GỬI EMAIL TỰ ĐỘNG (GMAIL SMTP)
# ===================================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# >>> THAY ĐỔI: Điền tài khoản Gmail dùng làm "tổng đài" gửi mã của nhóm bạn vào đây
spring.mail.username=nghianoel2k4@gmail.com

# >>> THAY ĐỔI: Nhập mã "Mật khẩu ứng dụng" (App Password) 16 ký tự của Google cấp vào đây
spring.mail.password=gdrq btmd rgbz lkym

# Cấu hình bảo mật đường truyền mã hóa TLS
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true